
def calculate_average(numbers):
    return sum(numbers) / len(numbers) if len(numbers) != 0 else "The empty list. Exit!"

def find_max(numbers):
    return max(numbers)

def find_min(numbers):
    return min(numbers)